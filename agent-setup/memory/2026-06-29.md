# 2026-06-29

## Сессия 1
Тема: установка скилла с SkillsMP

### Сделано
- Разобрались с сайтом skillsmp.com — каталог открытых скиллов для AI-агентов, есть бесплатный API (`/api/skills?search=...`)
- Выбрали и установили скилл `spreadsheet-ops` (1197 ⭐, автор AIPOCH) в `.claude/skills/spreadsheet-ops/` — анализ и обработка Excel/CSV: объединение файлов, чистка, статистика, формулы, форматирование, графики
- Скачали целиком: SKILL.md + 6 скриптов + 9 справочников
- Подняли pip через ensurepip, установили pandas 3.0.4 и openpyxl 3.1.5 (без них скрипты не работают)
- Проверили: все скрипты компилируются, библиотеки на месте
- Коммиты: `[agent] add: скилл spreadsheet-ops...`, `[agent] fix: убрать __pycache__...`

### Решения
- Из двух топ-скиллов выбрали spreadsheet-ops (работа с готовыми файлами), а не spreadsheet-automation (Google Sheets) — потому что Екатерина работает с выгрузками статистики WB и отчётами в Excel/CSV
- __pycache__ и *.pyc добавили в .gitignore

### TODO на потом
- Если основная работа идёт в Google Sheets, а не в файлах — можно докинуть скилл spreadsheet-automation
- Попробовать скилл на реальной выгрузке WB

## Сессия 2
Тема: скиллы из awesome-llm-apps

### Сделано
- Уточнили: репо называется `awesome-llm-apps` (не «11m»), скиллы в папке `awesome_agent_skills/`
- Проверили запрошенные content-creator и fullstack-developer — они УЖЕ стоят и побайтно идентичны репо (md5 совпали). Ставить было нечего — шаблон взял их из этого же репо
- Вместо них поставили 4 новых скилла из этого репо: `data-analyst`, `email-drafter`, `meeting-notes`, `project-planner` (каждый = один SKILL.md)
- Коммит: `[agent] add: 4 скилла из awesome-llm-apps...`

### Решения
- Не перезаписывать идентичные файлы ради галочки — сначала сверять md5/контент
- Подбор новых скиллов — под профиль Екатерины: продажи, отчёты, метрики WB, планирование проектов

## Сессия 3
Тема: блок «Моя команда» + разбор плагинов

### Сделано
- Объяснили, что Superpowers уже включён (settings.json), это методики работы, не «специалисты»
- Показали полный список скиллов (11 активных в .claude/skills/ + плагины)
- Нашли отдельную библиотеку `skills/custom/` (12) и `skills/global/` (10) — не активны, грузится только .claude/skills/
- Добавили в MEMORY.md блок «Моя команда (нейросотрудники)» — таблица из 11 активных специалистов с ролями + строка про резерв

### Решения
- «Команда» = только активные скиллы из .claude/skills/; библиотека skills/ — резерв, «нанимается» копированием папки в .claude/skills/
